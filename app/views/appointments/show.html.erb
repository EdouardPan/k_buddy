<div class="title-show">
  <h3 class="text-center">Consultations</h3>
  <h2 class="text-center category-show"><%= @appointment.category.upcase %></h2>
  <h6 class="text-center subtitle-show"><%= @appointment.medical_professional.location_name %> / <%= @appointment.medical_professional.doctor %></h6>
</div>

<div class="show-icon text-center">
  <%= render 'shared/icons', object: @appointment %>
</div>

<div class="separation-box text-center">
  <p>Début de la consultation:</p>
  <h4><%= @appointment.start_date.to_s(:long) %></h4>
  <br>
  <p>Fin de la consultation:</p>
  <h4><%= @appointment.end_date.to_s(:long) %></h4>
  <br>
  <p>Description:</p>
  <h6>
    <% if @appointment.description == "" %>
      "Pas de description"
    <% else %>
      <%= @appointment.description %>
    <% end %>
  </h6>
</div>

<div class="photo-box">

</div>
