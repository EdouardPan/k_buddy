<div class="title-show">
  <h3 class="text-center">Mes Symptômes</h3>
  <h2 class="text-center category-show"><%= @symptom.name.upcase %></h2>
</div>

<div class="show-icon text-center">
  <%= image_tag ("symptoms-icon.png"), class: "list-icon" %>
</div>

<div class="separation-box text-center">
  <p>Date de l'évènement:</p>
  <h4><%= @symptom.start_date.to_s(:long) %></h4>
  <br>
  <p>Intensité:</p>
  <h4><%= @symptom.intensity %></h4>
  <br>
  <p>Description:</p>
  <h6>
    <% if @symptom.description.nil? %>
      "Pas de description"
    <% else %>
      <%= @symptom.description %>
    <% end %>
  </h6>
</div>


